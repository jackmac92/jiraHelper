{
  "name": "cbiJira",
  "version": "1.1.0",
  "main": "bundle.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "rm -f bundle && npm run build",
    "build": "rollup -c"
  },
  "devDependencies": {
    "rollup": "^0.41.6"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "jira": "^0.9.2"
  }
}
