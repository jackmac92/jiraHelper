{
  "name": "cbiJira",
  "version": "1.1.1",
  "main": "bundle.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "rm -f bundle && npm run build",
    "build": "rollup -c",
    "postpublish": "git add bundle.js && git commit -m 'Post publish commit'"
  },
  "devDependencies": {
    "rollup": "^0.41.6"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "jira": "^0.9.2"
  }
}
